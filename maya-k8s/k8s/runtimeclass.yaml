apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
---
# Placeholder for Epic 2's second half. This does NOT work in kind --
# Kata needs KVM passthrough that kind's container-based nodes don't
# provide. Apply this only once you have a real/nested-virt-enabled
# cluster, after installing kata-deploy there.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: kata-containers
handler: kata
